server:
  port: 8080

spring:
  application:
    name: mcm-gateway-server

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true